{
  "name": "jurnalumum-jsonserver",
  "version": "1.0.0",
  "description": "jurnal umum mockup",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch ./db.json",
    "start-auth": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ahadsenja",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3",
    "jsonwebtoken": "^8.5.1"
  }
}
